# OpenAI API key for Voice Payment Check (/api/voice-risk)
# Used for Whisper transcription + GPT-4o-mini linguistic risk extraction.
# Set the same variable in Vercel Project Settings → Environment Variables.
OPENAI_API_KEY=
